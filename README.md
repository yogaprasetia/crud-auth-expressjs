These codes does not contain the MYSQL codes please make sure to create your database by the name of authentication and a table of users with its three fields, or you can copy the code from tutorial.
Before you run the project please consider installing all the node.js packages using "npm install"
